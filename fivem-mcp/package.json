{
  "name": "fivem-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "FiveAI FiveM debug MCP - the single workspace package: server resource, client binding and MCP contracts.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "pnpm run typecheck:http && pnpm run build:http",
    "build:http": "node scripts/build-http-mcp.mjs",
    "build:identity": "node scripts/build-identity.mjs",
    "build:contracts": "pnpm run build:identity && esbuild src/index.ts --bundle --format=esm --platform=node --outfile=dist/index.mjs",
    "build:entry": "pnpm run build:identity && esbuild src/cli/entry.ts --bundle --format=esm --platform=node --outfile=dist/entry.mjs --banner:js=\"import { createRequire as __cr } from 'module'; const require = __cr(import.meta.url);\" && node -e \"require('node:fs').copyFileSync('src/cli/windows-files.ps1', 'dist/windows-files.ps1')\"",
    "build:broker": "pnpm run build:identity && esbuild src/broker/main.ts --bundle --format=esm --platform=node --outfile=dist/broker.mjs --banner:js=\"import { createRequire as __cr } from 'module'; const require = __cr(import.meta.url);\"",
    "build:bundle": "node scripts/build-resource.mjs",
    "build:legacy": "pnpm run build:contracts && pnpm run build:entry && pnpm run build:broker && pnpm run build:bundle",
    "typecheck": "pnpm run build:identity && tsc --noEmit",
    "typecheck:http": "tsc -p http-mcp/tsconfig.json",
    "pack": "pnpm run build",
    "test": "pnpm run typecheck:http && node --test tests/http-mcp-artifact.test.mjs tests/http-mcp.test.mjs",
    "test:fixture": "node tests/run-mcp-suite-fixture.mjs"
  },
  "dependencies": {
    "@fastify/websocket": "11.3.0",
    "@modelcontextprotocol/sdk": "1.30.0",
    "fastify": "5.12.3",
    "ws": "8.21.3",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@types/node": "22.18.1",
    "@types/ws": "8.18.1",
    "esbuild": "0.28.2",
    "typescript": "5.9.3"
  }
}
