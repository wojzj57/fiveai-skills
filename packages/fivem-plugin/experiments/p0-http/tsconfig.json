{
  "//": [
    "Typecheck floor for the P0 experiment only. It mirrors packages/mcp/tsconfig.json",
    "so the resource-side TypeScript is held to the same strictness as the MCP package.",
    "Emitting is esbuild's job (scripts/build-p0-experiment.mjs); tsc only checks."
  ],
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": ["node"]
  },
  "include": ["src/**/*.ts"]
}
