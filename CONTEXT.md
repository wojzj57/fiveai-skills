# Ubiquitous Language

本词汇表描述 `fivem-mcp` 的 HTTP MCP 服务、FiveM 客户端绑定和日志读取。英文标识符保留在括号中，便于对照工具输入与输出。

## MCP 会话与工具

| 术语 | 定义 | 避免混用的说法 |
| --- | --- | --- |
| **MCP 调用端** | 通过 Streamable HTTP 与 `fivem-mcp` 通信的外部工具调用者。 | 客户端、游戏客户端 |
| **MCP 会话** | MCP 调用端初始化后，由当前服务实例识别的一段请求上下文。 | 客户端连接、绑定 |
| **工具发现** | 某次 `tools/list` 请求返回当时已注册工具及其可用性说明的快照。 | 工具授权、永久工具列表 |
| **工具调用** | MCP 调用端通过 `tools/call` 对一个工具发起的请求。 | 会话、任务 |
| **资源纪元** (`resourceEpoch`) | `fivem-mcp` 每次运行实例的身份，用于区分资源重启前后的绑定和任务证据。 | 会话 ID、客户端纪元 |

## FiveM 运行目标

| 术语 | 定义 | 避免混用的说法 |
| --- | --- | --- |
| **FiveM 资源** | 由 FXServer 发现并管理启停状态的具名脚本单元。 | MCP 资源、日志来源 |
| **游戏客户端** | 加入 FXServer 并运行 `fivem-mcp` 客户端脚本的 FiveM 游戏实例。 | MCP 调用端 |
| **客户端绑定** | 服务端与某个游戏客户端就资源纪元、连接身份和客户端纪元达成的当前关联。 | MCP 会话、日志源 |
| **就绪客户端** | 当前绑定已收到 Lua 与 JavaScript 心跳确认的游戏客户端。 | 已连接客户端、可读日志客户端 |
| **执行侧** (`side`) | 工具请求指定的运行或读取范围，取值为 `server`、`client`，日志查询还可取 `all`。 | 连接状态 |

## 日志读取

| 术语 | 定义 | 避免混用的说法 |
| --- | --- | --- |
| **客户端日志目录** | 配置给 FXServer、供日志采集器读取游戏客户端日志文件的目录。 | 游戏客户端资源目录 |
| **本机日志桥** | 在游戏客户端所在机器读取 `CitizenFX` 日志并向 FXServer 回环监听器提交有界批次的本地进程。 | 游戏客户端、MCP 调用端 |
| **绑定标记** | 当前客户端绑定写入其日志文件的唯一归属字符串。 | 日志内容、心跳 |
| **客户端日志源** | 与当前绑定标记唯一匹配的可读客户端日志文件或本机日志桥数据流。 | 就绪客户端 |
| **日志覆盖状态** (`coverage`) | 采集器对指定执行侧及客户端日志来源完整性的当前判断。 | 日志行数、客户端连接状态 |
| **日志记录** | 采集器保存的一条带执行侧、资源名和通道等信息的输出。 | 日志源、绑定标记 |
| **资源过滤条件** (`logs.resource`) | `logs` 查询中按日志记录所属 FiveM 资源名筛选的条件。 | `resource` 工具操作、MCP 资源 |

## 关系

- 一个 **MCP 会话** 属于一个运行中的服务实例；资源重启后，旧会话不能用于新实例。
- 一个 **游戏客户端** 在同一资源纪元内至多有一个当前 **客户端绑定**；绑定需要 Lua 与 JavaScript 心跳才能成为 **就绪客户端**。
- **就绪客户端** 只证明可以选择该执行目标，不证明存在可读的 **客户端日志源**。
- 一个可用的 **客户端日志源** 必须有可访问的文件或持续运行的 **本机日志桥**，并与当前 **绑定标记** 唯一匹配。
- 使用 **本机日志桥** 时，桥读取日志文件并提交当前 **绑定标记**；FXServer 无需直接读取客户端日志目录。
- `logs` 的 `side:"all"` 分别报告服务端和选定客户端的 **日志覆盖状态**；服务端日志不能充当客户端日志。
- **资源过滤条件** 只筛选已有 **日志记录**，不会使不可用的日志源变得可用。

## 对话示例

> **开发者：** “`status` 显示一个 **就绪客户端**，为什么 `logs({side:"client"})` 仍返回 `LOG_SOURCE_UNAVAILABLE`？”
>
> **领域专家：** “**客户端绑定** 已就绪，但 **客户端日志源** 还需要可访问的文件或运行中的 **本机日志桥**，并匹配唯一的 **绑定标记**。先看 **日志覆盖状态**。”
>
> **开发者：** “覆盖状态是 `unconfigured`；加上 `resource:"exnui"` 能查到吗？”
>
> **领域专家：** “不能。**资源过滤条件** 只作用于已采集的 **日志记录**。若目录被宿主沙箱拒绝，可在本机运行 **本机日志桥**。”
>
> **开发者：** “资源重启后旧请求报 `unknown or expired session`，这与日志源有关吗？”
>
> **领域专家：** “无关。重新初始化 **MCP 会话** 后再看 `status`；旧会话属于上一个服务实例。”

## 待澄清的歧义

- “客户端”同时指 **MCP 调用端** 和 **游戏客户端**；讨论连接、日志与执行目标时应写完整名称。
- “连接”可能指 HTTP 会话存在、游戏客户端已加入或 **客户端绑定** 已就绪；应分别使用 **MCP 会话**、**游戏客户端**、**就绪客户端**。
- “日志可用”可能指服务端控制台采集可用，或某个 **客户端日志源** 可用；应明确执行侧及 **日志覆盖状态**。
- “资源”可能指 **FiveM 资源**、`resource` 工具或 `logs.resource` 过滤条件；工具名和参数名应带代码格式，业务概念使用 **FiveM 资源**。
- `available` 用于工具说明和日志覆盖时含义不同；应分别说“工具当前可调用”和“日志源覆盖为 `available`”。
